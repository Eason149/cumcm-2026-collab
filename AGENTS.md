# AGENTS.md

本仓库用于全国大学生数学建模竞赛团队协作。所有 Codex 任务默认遵守以下规则。

## 建模原则

- 先识别问题类型和数据结构，再选择模型；必须解释模型为何适合。
- 优先使用简单、稳定、可解释的 baseline；复杂模型只有在验证后确有增益时采用。
- 不为展示复杂性堆砌模型，不把算法名称当作创新。
- 目标函数、约束、变量、参数和单位必须与题意一致。
- 统计模型检查假设和残差；机器学习使用合适的样本外验证；优化模型检查全部约束和最优性信息。
- 启发式算法固定随机种子，报告关键参数，并进行多次独立运行。

## 数据与代码

- `data/raw/` 中的原始文件视为只读，不覆盖、不格式化。
- 中间数据写入 `data/processed/`，图表和表格分别写入 `results/figures/` 与 `results/tables/`。
- 关键算法封装为函数，明确输入、输出、单位和边界条件。
- 论文中的关键数字应由保存的结果文件生成，不手工编造或默写。
- 不提交密码、令牌、个人信息、缓存、虚拟环境或大型临时文件。

## 协作与修改

- 修改前检查工作区状态，保留其他队员已有且无关的更改。
- 每个分支聚焦一个问题或一类修改；不要在同一提交中混入无关格式化。
- 对公式、目标函数、数据口径或结论的重大改变，必须在 Pull Request 中写明原因和影响。
- 不删除其他队员文件；确需删除时先在团队内确认。

## 论文质量

- 摘要包含问题、核心模型设计、关键数值结果、验证和结论。
- 所有重要公式定义符号，并说明其现实含义。
- 图表应有标题、坐标标签、单位和正文解释。
- 明确模型局限，不把相关关系写成因果关系。
- 优秀论文只用于学习，不复制或改写其表达、图表和结果。
