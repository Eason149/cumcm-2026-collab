# 协作规范

## 分支命名

- `model/problem-1`：模型建立与求解
- `data/cleaning`：数据清洗与探索
- `paper/abstract`：论文内容
- `fix/result-check`：结果或一致性修正

## Pull Request 内容

每个 Pull Request 至少说明：

1. 修改解决了什么问题；
2. 采用了什么方法，为什么适合；
3. 生成了哪些结果文件；
4. 如何验证；
5. 是否影响论文中的公式、表格或结论。

## 审查重点

- 数据口径和单位是否一致；
- 公式与代码是否一致；
- 约束是否在程序中真实实现；
- 是否存在数据泄漏或重复计数；
- 随机结果是否可复现；
- 图表和摘要数字是否来自同一版本结果。

## 冲突处理

论文、结果表格发生冲突时，不直接选择“ours”或“theirs”。先核对最新代码输出和实验配置，再由结果反向更新论文。
